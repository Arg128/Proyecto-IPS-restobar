{
  "buildCommand": "cd ../.. && CI=false npx turbo run build --filter=admin-frontend...",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "ignoreCommand": "npx turbo-ignore admin-frontend",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
