{
  "buildCommand": "cd ../.. && CI=false npx turbo run build --filter=cocina-frontend...",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "ignoreCommand": "npx turbo-ignore cocina-frontend",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
