{
  "buildCommand": "cd ../.. && CI=false npx turbo run build --filter=caja-frontend...",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "ignoreCommand": "npx turbo-ignore caja-frontend",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
