{
  "buildCommand": "cd ../.. && CI=false npx turbo run build --filter=delivery-frontend...",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "ignoreCommand": "npx turbo-ignore delivery-frontend",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
