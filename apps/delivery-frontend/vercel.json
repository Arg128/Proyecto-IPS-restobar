{
  "buildCommand": "cd ../.. && npx turbo run build --filter=delivery-frontend...",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
