{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "db:migrate": "pnpm --filter @restobar/database db:migrate",
    "db:seed": "pnpm --filter @restobar/database db:seed",
    "db:generate": "pnpm --filter @restobar/database db:generate"
  },
  "devDependencies": {
    "globals": "^17.7.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "overrides": {
      "jest": "24.9.0"
    }
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "@tursodatabase/database": "^0.6.1"
  }
}
